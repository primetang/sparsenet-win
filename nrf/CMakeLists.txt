CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

PROJECT(SPARSENET)

INCLUDE_DIRECTORIES(
    ${SPARSENET_SOURCE_DIR}/include
)

SET(LIBRARY_OUTPUT_PATH ${SPARSENET_BINARY_DIR}/lib)

ADD_SUBDIRECTORY("src")
ADD_SUBDIRECTORY("matlab")